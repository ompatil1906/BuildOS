# Sandbox Runner

Placeholder service for future isolated execution of generated code. Production upgrades should use locked-down containers or microVMs, strict network policy, resource limits, read-only mounts, and per-run credentials.

